# Лабораторна робота №2: ООП на C++

## Опис проекту

Система управління періодичними виданнями з демонстрацією всіх основних концепцій ООП.

## Виконання вимог завдання

### ✅ Пункт 1: Два користувацькі класи (не менше 8 полів)

**Клас Publication (9 полів):**

- `id_` (int) - унікальний ідентифікатор
- `title_` (string) - назва видання
- `description_` (string) - опис
- `price_` (double) - ціна за місяць
- `category_` (string) - категорія
- `rating_` (double) - рейтинг
- `review_count_` (int) - кількість відгуків
- `pages_` (int) - кількість сторінок

**Клас Client (9 полів):**

- `client_id_` (int) - ID клієнта
- `first_name_` (string) - ім'я
- `last_name_` (string) - прізвище
- `email_` (string) - електронна пошта
- `phone_` (string) - телефон
- `is_blacklisted_` (bool) - статус чорного списку
- `balance_` (double) - баланс рахунку
- `subscription_months_` (int) - місяців підписки
- `subscribed_publication_` (Publication) - підписане видання

### ✅ Пункт 2: 3 загальні та 5 приватних елементів

**Publication:**

- Загальні: конструктор, деструктор, `DisplayInfo()`
- Приватні: всі 8 полів даних + геттери

**Client:**

- Загальні: конструктор, деструктор, `DisplayInfo()`
- Приватні: всі 9 полів даних + геттери

### ✅ Пункт 3: Зв'язок об'єктів без дружніх функцій

У класі `Client` є поле `subscribed_publication_` типу `Publication` - це прямий зв'язок між класами.

### ✅ Пункт 4: 6 методів (2 перевантажені)

**Publication:**

1. `SetPrice(double)` / `SetPrice(double, double)` - перевантажений
2. `AddRating(double)` / `AddRating(int, int)` - перевантажений
3. `CalculateDiscount(const Publication&)` - приймає об'єкт
4. `CreateCopy()` - повертає об'єкт
5. `CalculateTotalPrice(int)`
6. `IsAvailable()`

**Client:**

1. `AddBalance(double)` / `AddBalance(double, double)` - перевантажений
2. `Subscribe(const Publication&, int)` / `Subscribe(const Publication&)` - перевантажений
3. `PurchaseSubscription(const Publication&, int)` - приймає об'єкт
4. `GetSubscribedPublication()` - повертає об'єкт
5. `CalculateDiscount(int)`
6. `CanAfford(const Publication&, int)`

### ✅ Пункт 5: Методи для файлів

Обидва класи мають:

- `SaveToFile(std::ofstream&)` - запис у файл
- `LoadFromFile(std::ifstream&)` - читання з файлу

### ✅ Пункт 6: 5 статичних + 5 динамічних об'єктів

У `main()`:

- 5 статичних: `static_pub1`, `static_pub2`, `static_client1`, `static_client2`, `static_client3`
- 5 динамічних: `dyn_pub1`, `dyn_pub2`, `dyn_pub3`, `dyn_client1`, `dyn_client2`

### ✅ Пункт 7-8: 2 масиви об'єктів

- Масив `Publication publications[3]`
- Масив `Client clients[3]`
- Демонстрація роботи з масивами у функції `DemonstrateArrays()`

### ✅ Пункт 9: Демонстрація всіх методів

Всі методи демонструються у функціях:

- `DemonstrateOverloadedMethods()`
- `DemonstrateObjectMethods()`
- `DemonstrateFileSaveLoad()`
- `DemonstrateArrays()`

### ✅ Пункт 10: Сценарій взаємодії об'єктів

Функція `DemonstrateInteraction()` показує як клієнт купує підписку:

- Клієнт вибирає видання
- Перевіряється баланс
- Списуються кошти
- Оформлюється підписка

### ✅ Пункт 11: Використання покажчиків

Функція `DemonstratePointers()`:

- Створення об'єкта через `new`
- Робота через покажчик `->`
- Звільнення пам'яті через `delete`

### ✅ Пункт 12: Динамічна пам'ять та сортування

Методи:

- `Publication::GenerateAndSortRatings()` - генерує випадковий масив рейтингів та сортує
- `Client::GenerateAndSortMonths()` - генерує випадковий масив місяців та сортує

### ✅ Пункт 13: Коментарі в коді

Кожен пункт завдання позначений коментарями в коді:

```cpp
// Пункт 1: Клас Publication з не менше 8 полів
// Пункт 2: 5 приватних елементів
// і т.д.
```

### ✅ Пункт 14: Google C++ Style Guide

Код дотримується вимог:

- Змінні у форматі `snake_case_` з підкресленням для приватних
- Імена класів у `PascalCase`
- Імена функцій у `PascalCase`
- Відступи 2 пробіли
- Максимальна довжина рядка 80 символів

### ✅ Пункт 15: Три файли

Структура проекту:

- `Publication.h` - заголовний файл класу Publication
- `publications.cpp` - реалізація класу Publication
- `Client.h` - заголовний файл класу Client
- `clients.cpp` - реалізація класу Client
- `main.cpp` - демонстраційна програма

## Компіляція та запуск

```bash
# Компіляція
make

# Запуск
./lab2_demo

# Очищення
make clean
```

## Структура файлів

```
lab2/
├── Publication.h          # Заголовок класу Publication
├── publications.cpp       # Реалізація класу Publication
├── Client.h              # Заголовок класу Client
├── clients.cpp           # Реалізація класу Client
├── main.cpp              # Демонстраційна програма
├── Makefile             # Для компіляції
├── README.md            # Цей файл
└── .gitignore          # Налаштування Git
```

## Результати виконання

Програма демонструє:

1. Створення об'єктів у статичній та динамічній пам'яті
2. Роботу з масивами об'єктів
3. Перевантаження методів
4. Методи, що приймають та повертають об'єкти
5. Збереження та завантаження з файлів
6. Взаємодію між об'єктами (клієнт купує підписку)
7. Роботу з покажчиками
8. Динамічне виділення пам'яті та сортування

## Особливості реалізації

- **Інкапсуляція**: всі поля приватні, доступ через методи
- **Перевантаження методів**: різні варіанти для зручності
- **Композиція**: клас Client містить об'єкт Publication
- **Управління пам'яттю**: коректне виділення та звільнення
- **Валідація даних**: перевірка коректності введених значень
- **Сортування**: власна реалізація алгоритмів сортування

## Автор

Створено для виконання лабораторної роботи №2 з ООП

## Функціонал системи

### 🔧 Основні можливості:

1. **Управління виданнями:**

   - Додавання/видалення періодичних видань
   - Пошук за категоріями та ключовими словами
   - Система рейтингів на основі відгуків

2. **Управління клієнтами:**

   - Реєстрація нових клієнтів
   - Валідація email та телефону
   - Чорний список неплатників

3. **Система замовлень:**

   - Створення заявок на підписку
   - Розрахунок вартості з урахуванням знижок
   - Відстеження термінів оплати

4. **Система відгуків:**

   - Клієнти можуть залишати відгуки
   - Автоматичний розрахунок рейтингу видань
   - Модерація відгуків

5. **Система повідомлень:**

   - Нагадування про закінчення підписки
   - Повідомлення про необхідність оплати
   - Пропозиції знижок

6. **Система знижок:**
   - Знижки за замовлення кількох видань
   - Знижки за довгострокову підписку
   - Автоматичний розрахунок

## Структура класів

### 📋 Основні класи:

- **`Publication`** - представляє періодичне видання
- **`Client`** - представляє клієнта системи
- **`Order`** - представляє замовлення/заявку
- **`Review`** - представляє відгук на видання
- **`Notification`** - представляє повідомлення клієнту
- **`PublicationSystem`** - головний клас управління системою

### 🎯 Ключові концепції ООП:

1. **Інкапсуляція:**

   - Приватні поля даних
   - Публічні методи доступу (геттери/сеттери)
   - Контроль валідності даних

2. **Абстракція:**

   - Логічне групування функціональності в класах
   - Приховування деталей реалізації

3. **Поліморфізм:**
   - Перевантаження операторів
   - Перевантаження методів

## Компіляція та запуск

### Використання Makefile:

```bash
# Компіляція
make

# Запуск програми
make run

# Компіляція в режимі налагодження
make debug

# Очищення файлів
make clean

# Перевірка коду
make check
```

### Ручна компіляція:

```bash
g++ -std=c++11 -Wall -Wextra -O2 -o publication_system main.cpp Publication.cpp Client.cpp Order.cpp
```

## Структура файлів

```
lab2/
├── main.cpp                 # Головний файл з меню
├── Publication.h/.cpp       # Клас періодичного видання
├── Client.h/.cpp           # Клас клієнта
├── Order.h/.cpp            # Клас замовлення
├── Review.h               # Клас відгуку (заголовок)
├── Notification.h         # Клас повідомлення (заголовок)
├── PublicationSystem.h    # Головний клас системи (заголовок)
├── Makefile              # Файл для компіляції
└── README.md             # Цей файл
```

## Приклад використання

1. **Запустіть програму:**

   ```bash
   make run
   ```

2. **Ініціалізуйте тестові дані:**

   - Виберіть опцію "9" в головному меню

3. **Створіть клієнта:**

   - Меню "Управління клієнтами" → "Додати клієнта"

4. **Створіть замовлення:**

   - Меню "Управління замовленнями" → "Створити замовлення"
   - Додайте видання до замовлення
   - Оплатіть замовлення

5. **Залиште відгук:**
   - Головне меню → "Додати відгук на видання"

## Особливості реалізації

### 🛡️ Безпека та валідація:

- Перевірка валідності email за допомогою regex
- Валідація номерів телефонів
- Контроль діапазонів для рейтингів (1-5)
- Перевірка дат та термінів

### 📊 Управління даними:

- Використання STL контейнерів (vector, map)
- Автоматична генерація унікальних ID
- Робота з датами через chrono

### 🎨 Інтерфейс користувача:

- Інтерактивне консольне меню
- Детальне виведення інформації
- Обробка помилок вводу

## Розширення функціональності

Система спроектована для легкого розширення:

1. **Додавання нових типів видань**
2. **Інтеграція з базами даних**
3. **Додавання GUI інтерфейсу**
4. **Система платежів**
5. **Веб-інтерфейс**

## Вимоги до системи

- **Компілятор:** GCC/Clang з підтримкою C++11
- **ОС:** Linux, macOS, Windows (з MinGW)
- **Пам'ять:** Мінімальні вимоги
- **Додаткові бібліотеки:** Тільки стандартна бібліотека C++

## Автор

Створено для виконання лабораторної роботи №2 з ООП

## Ліцензія

Навчальний проект для академічних цілей
